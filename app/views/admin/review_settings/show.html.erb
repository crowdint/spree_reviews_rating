<%= render :partial => 'admin/shared/configuration_menu' %>

<h1><%= t("spree_reviews.review_settings") %></h1>

<table>
  <tr>
    <th scope="row"><%= t("spree_reviews.include_unapproved") %>:</th>
    <td><%= Reviews::Config[:include_unapproved_reviews] ? t('yes') : t('no') %></td>
  </tr>
  <tr>
    <th scope="row"><%= t("spree_reviews.feedback_rating") %>:</th>
    <td><%= Reviews::Config[:feedback_rating] ? t('yes') : t('no') %></td>
  </tr>
  <tr>
    <th scope="row"><%= t("spree_reviews.show_email") %>:</th>
    <td><%= Reviews::Config[:show_email] ? t('yes') : t('no') %></td>
  </tr>
  <tr>
    <th scope="row"><%= t("spree_reviews.require_login") %>:</th>
    <td><%= Reviews::Config[:require_login] ? t('yes') : t('no') %></td>
  </tr>
  <tr>
    <th scope="row"><%= t("spree_reviews.preview_size") %>:</th>
    <td><%= Reviews::Config[:preview_size] %></td>
  </tr>
</table>

<p>
  <%= link_to_with_icon('edit', t("edit"), edit_admin_review_settings_path) %>
</p>
